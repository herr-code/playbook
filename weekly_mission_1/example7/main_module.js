/* Importando el módulo */

// Como es node 12 se debe correr asi para que no de error
// node --experimental-modules index.js 
import * as loggerModule from './logger.js'

console.log(loggerModule)
